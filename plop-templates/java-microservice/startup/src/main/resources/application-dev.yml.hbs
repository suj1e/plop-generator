logging:
  level:
    sql: DEBUG
    com.alibaba.nacos: OFF # nacos 心跳日志屏蔽
    org.springframework.boot: DEBUG
    root: INFO
    org.flooc.combo: TRACE
    org.flooc.combox: TRACE
    {{groupId}}: DEBUG
  # structured:
    # format:
      # file: ecs
  file:
    name: ./logs/${spring.application.name}/app-dev.log
spring:
  task:
    execution:
      pool:
        core-size: 4
        max-size: 50
        queue-capacity: 10000
    scheduling:
      pool:
        size: 2