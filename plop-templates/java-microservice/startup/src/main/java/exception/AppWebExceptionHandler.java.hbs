package {{groupId}}.exception;

import org.flooc.cox.boot.web.logging.AppLogging;
import org.flooc.cox.boot.web.exception.AbstractWebExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/**
 * 注意先后捕获顺序
 *
 * @author sujie
 * @since 1.0.0
 */
@AppLogging(enable = false)
@RestControllerAdvice("{{groupId}}.presentation")
public class AppWebExceptionHandler extends AbstractWebExceptionHandler {

}